rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow absolutely anyone to read/write
      allow read, write: if true;
    }
  }
}